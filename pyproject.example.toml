[project]
name = "my-configured-app"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "flegmconfig>=0.2.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"